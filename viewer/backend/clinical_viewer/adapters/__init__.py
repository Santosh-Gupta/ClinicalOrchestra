"""Adapters convert a source of harness activity into the unified Event stream.

- :mod:`replay` reconstructs a timeline from on-disk run artifacts (MVP).
- :mod:`live` defines the contract for a future in-process event emitter.
"""
